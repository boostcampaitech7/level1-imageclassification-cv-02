data_module: "src.data.custom_datamodules.sketch_datamodule.SketchDataModule" # data module path
model_module: "src.plmodules.sketch_module.SketchModelModule" # pl module path

data_config_path: "configs/data_configs/sketch_config.yaml" # data config path
checkpoint_path: "checkpoints/epoch=4-step=470.ckpt"   # checkpoint path

name: ""

model:
  model_name: "eva02_large_patch14_448.mim_m38m_ft_in22k_in1k"
  num_classes: 500
  pretrained: True

trainer:
  accelerator: gpu
  devices: 1
  default_root_dir: "output"  # outputs 폴더에 저장합니다.

seed: 42  # 시드 값 추가
